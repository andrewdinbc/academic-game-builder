export const metadata = { title:"academic-game-builder", description:"Educational complexity-scaled video game builder — creates browser games tied to curriculum outcomes." };
export default function RootLayout({ children }) {
  return (<html lang="en"><body>{children}</body></html>);
}
